# API Keys & Environment

## .env file
Location: `read-rd/.env` (gitignored)

Contains:
- `GEMINI_API_KEY` — Google Gemini API key for gemini-audit.py

The `.env` file is auto-loaded by `.project/gemini-audit.py`.
